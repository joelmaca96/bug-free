<?xml version="1.0" encoding="UTF-8"?>
<svg width="1024" height="1024" viewBox="0 0 1024 1024" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="title desc">
  <title id="title">apoteke — cross logo (dark mode)</title>
  <desc id="desc">Minimal rounded cross with turquoise-to-cyan gradient for dark theme. Transparent background.</desc>
  <defs>
    <linearGradient id="g" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#003344"/>
      <stop offset="45%" stop-color="#005E66"/>
      <stop offset="100%" stop-color="#66D9FF"/>
    </linearGradient>
    <filter id="soft" x="-40%" y="-40%" width="180%" height="180%">
      <feDropShadow dx="0" dy="10" stdDeviation="30" flood-color="#000000" flood-opacity="0.35"/>
    </filter>
  </defs>

  <!-- Transparent background (use on dark surfaces) -->
  <rect width="100%" height="100%" fill="transparent"/>

  <!-- Horizontal arm -->
  <rect x="212" y="384" width="600" height="256" rx="64" ry="64" fill="url(#g)" filter="url(#soft)"/>

  <!-- Vertical arm -->
  <rect x="384" y="212" width="256" height="600" rx="64" ry="64" fill="url(#g)" filter="url(#soft)"/>

  <!-- Optional subtle inner highlight -->
  <rect x="240" y="412" width="544" height="200" rx="56" ry="56" fill="rgba(255,255,255,0.03)"/>
</svg>
